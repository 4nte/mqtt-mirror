apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mqtt-mirror.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "mqtt-mirror.name" . }}
    helm.sh/chart: {{ include "mqtt-mirror.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "mqtt-mirror.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
